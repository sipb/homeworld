# general dependencies
debhelper

# homeworld-etcd
git

# homeworld-knc
libkrb5-dev

# homeworld-admin-tools
libarchive-tools, zip, python3, python3-yaml, python3-requests

# homeworld-hyperkube
rsync

# homeworld-rkt
autoconf, automake, bc, binutils, bison, cpio, file, flex, git, gnupg, libacl1-dev, libcap-dev, libelf-dev, libglib2.0-dev, libpcre3-dev, libpixman-1-dev, libreadline-dev, libssl-dev, libsystemd-dev, libxml2, libxml2-dev, locales, openssl, patch, pkg-config, python, python3, realpath, squashfs-tools, wget, xz-utils, zlib1g-dev, zlibc

# debian ACIs
debootstrap, devscripts, fakeroot, fakechroot

# dnsmasq-nanny ACI build, other ACI builds
systemd-container

# generic
build-essential

# miscellaneous
bzip2, ca-certificates, curl, gzip, less, sudo

# glass
python3-jsonschema, reprepro, python3-apt

# uploading
python-crcmod, python-pip, python-setuptools, python-wheel, rsync, ssh

# killall
psmisc

# bazel
openjdk-8-jdk, g++, unzip
