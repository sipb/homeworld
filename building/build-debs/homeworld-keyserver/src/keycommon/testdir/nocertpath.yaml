
keyserver: localhost
authoritypath: testdir/cert.pem

keypath: testdir/grant.key

# irrelevant information to test how loading works

tokenpath: /etc/hyades/keyclient/bootstrap_token.txt
tokenapi: renew-keygrant
request-initial-keygrant: renew-keygrant
