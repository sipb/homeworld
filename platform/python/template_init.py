# this is a template file that gets instantiated for any bazel package with python-accessible resources.
# its simple presence as __init__.py is enough to make a package importable, and therefore able to be referenced by
# pkgutil.get_data.
